[package]
name = "pager"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5.60", features = ["derive"] }
crossterm = { version = "0.28", features = ["use-dev-tty"] }
regex = "1.11"
rustix = { version = "1.0", features = ["std", "termios"] }
rustix-openpty = "0.2"
